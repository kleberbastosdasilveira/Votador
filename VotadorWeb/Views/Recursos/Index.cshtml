@model IEnumerable<VotadorWeb.ViewModels.RecursoViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-hover">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TituloRecurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescricaoRecurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroVotacao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TituloRecurso)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DescricaoRecurso)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NumeroVotacao)
                </td>
                <td class="text-right">
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>
